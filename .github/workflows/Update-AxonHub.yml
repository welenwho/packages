name: 每日检查并更新 Axonhub 核心

on:
  workflow_dispatch:
    inputs:
      build_apks:
        description: 更新后编译并发布 APK
        required: false
        default: true
        type: boolean
  workflow_call:
    inputs:
      build_apks:
        description: 是否在此工作流内编译并发布 APK
        required: false
        default: true
        type: boolean
    outputs:
      package_changed:
        description: AxonHub 软件包是否发生变更
        value: ${{ jobs.release.outputs.package_changed }}
      packages:
        description: 需要编译的软件包目录
        value: ${{ jobs.release.outputs.packages }}
      source_sha:
        description: 更新后的精确提交 SHA
        value: ${{ jobs.release.outputs.source_sha }}

permissions:
  contents: write

concurrency:
  group: repository-maintenance
  cancel-in-progress: false

jobs:
  release:
    name: 检查并更新 Axonhub 核心
    runs-on: ubuntu-latest
    timeout-minutes: 120
    outputs:
      package_changed: ${{ steps.package_commit.outputs.changed }}
      packages: ${{ steps.package_commit.outputs.packages }}
      source_sha: ${{ steps.package_commit.outputs.commit_sha }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: 检出软件包仓库
        uses: actions/checkout@main
        with:
          fetch-depth: 0
          ref: ${{ github.event.repository.default_branch }}

      - name: 同步默认分支
        env:
          PACKAGE_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          source .github/scripts/ci-common.sh
          ci_sync_branch "$PACKAGE_BRANCH"

      - name: 检查 Axonhub 上游提交
        id: version
        run: |
          upstream_branch='unstable'
          makefile='axonhub/Makefile'
          upstream_commit="$(git ls-remote \
            https://github.com/looplj/axonhub.git \
            "refs/heads/$upstream_branch" | cut -f1)"
          [[ "$upstream_commit" =~ ^[0-9a-f]{40}$ ]]

          packaged_commit="$(sed -n 's/^AXONHUB_UPSTREAM_COMMIT:=//p' "$makefile")"
          if [[ "$packaged_commit" == "$upstream_commit" ]]; then
            package_version="$(sed -n 's/^PKG_VERSION:=//p' "$makefile")"
            version="$(sed -n 's/^AXONHUB_BUILD_VERSION:=//p' "$makefile")"
          else
            package_version="$(TZ=Asia/Shanghai date +%Y%m%d)"
            version="${upstream_branch}-${package_version}"
          fi

          release_tag="axonhub-${version}"
          changed=true
          if [[ "$packaged_commit" == "$upstream_commit" ]] && \
             gh release view "$release_tag" \
               --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            changed=false
            echo "Axonhub 上游无更新：$upstream_commit"
            echo '### Axonhub 上游无更新，本次任务结束。' >> "$GITHUB_STEP_SUMMARY"
          fi

          {
            echo "changed=$changed"
            echo "upstream_branch=$upstream_branch"
            echo "package_version=$package_version"
            echo "version=$version"
            echo "release_tag=$release_tag"
            echo "upstream_commit=$upstream_commit"
          } >> "$GITHUB_OUTPUT"

      - name: 检出 Axonhub 源码
        uses: actions/checkout@main
        with:
          repository: looplj/axonhub
          ref: ${{ steps.version.outputs.upstream_commit }}
          fetch-depth: 20
          path: upstream

      - name: 生成 Axonhub 发布说明
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
          UPSTREAM_BRANCH: ${{ steps.version.outputs.upstream_branch }}
        run: |
          notes_file="$RUNNER_TEMP/axonhub-release-notes.md"
          {
            echo "版本：$BUILD_VERSION"
            echo "上游分支：$UPSTREAM_BRANCH"
            echo '支持架构：AMD64, ARM64, LoongArch64'
            echo
            echo '最近三次提交'
            git -C upstream log -3 --format='%h %s'
          } > "$notes_file"

      - name: 配置 pnpm
        if: steps.version.outputs.changed == 'true'
        uses: pnpm/action-setup@master
        with:
          version: 10

      - name: 配置 Node.js
        if: steps.version.outputs.changed == 'true'
        uses: actions/setup-node@main
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: upstream/frontend/pnpm-lock.yaml

      - name: 配置 Go
        if: steps.version.outputs.changed == 'true'
        uses: actions/setup-go@main
        with:
          go-version-file: upstream/go.mod
          cache-dependency-path: upstream/go.sum

      - name: 编译 Linux 核心
        if: steps.version.outputs.changed == 'true'
        id: build
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
        run: |
          output_dir="$GITHUB_WORKSPACE/release"
          commit="$(git -C upstream rev-parse HEAD)"
          build_time="$(git -C upstream show -s --format=%cI HEAD)"
          mkdir -p "$output_dir"

          pnpm --dir upstream/frontend install --frozen-lockfile
          make -C upstream build-frontend
          make -C upstream generate-schema

          ldflags='-s -w'
          ldflags+=" -X github.com/looplj/axonhub/internal/build.Version=$BUILD_VERSION"
          ldflags+=" -X github.com/looplj/axonhub/internal/build.Commit=$commit"
          ldflags+=" -X github.com/looplj/axonhub/internal/build.BuildTime=$build_time"

          for arch in amd64 arm64 loong64; do
            (
              cd upstream
              CGO_ENABLED=0 GOOS=linux GOARCH="$arch" \
                go build -trimpath -tags=nomsgpack -ldflags "$ldflags" \
                -o "$output_dir/axonhub_linux_$arch" ./cmd/axonhub
            )
            chmod 0755 "$output_dir/axonhub_linux_$arch"
            printf '%s_hash=%s\n' \
              "$arch" \
              "$(sha256sum "$output_dir/axonhub_linux_$arch" | cut -d' ' -f1)" \
              >> "$GITHUB_OUTPUT"
          done

          "$output_dir/axonhub_linux_amd64" version | grep -F "$BUILD_VERSION"
          test "$(find "$output_dir" -maxdepth 1 -type f -name 'axonhub_linux_*' | wc -l)" -eq 3

      - name: 发布 Axonhub 核心
        if: steps.version.outputs.changed == 'true'
        env:
          RELEASE_TAG: ${{ steps.version.outputs.release_tag }}
        run: |
          notes_file="$RUNNER_TEMP/axonhub-release-notes.md"
          assets=(
            release/axonhub_linux_amd64
            release/axonhub_linux_arm64
            release/axonhub_linux_loong64
          )

          if gh release view "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" "${assets[@]}" \
              --repo "$GITHUB_REPOSITORY" --clobber
            gh release edit "$RELEASE_TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --title "$RELEASE_TAG" \
              --notes-file "$notes_file" \
              --prerelease=false
          else
            gh release create "$RELEASE_TAG" "${assets[@]}" \
              --repo "$GITHUB_REPOSITORY" \
              --target "$GITHUB_SHA" \
              --title "$RELEASE_TAG" \
              --notes-file "$notes_file"
          fi

      - name: 同步 Axonhub 发布信息
        if: steps.version.outputs.changed != 'true'
        env:
          RELEASE_TAG: ${{ steps.version.outputs.release_tag }}
        run: |
          gh release edit "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --title "$RELEASE_TAG" \
            --notes-file "$RUNNER_TEMP/axonhub-release-notes.md" \
            --prerelease=false

      - name: 更新 Axonhub 软件包元数据
        if: steps.version.outputs.changed == 'true'
        id: package_commit
        env:
          VERSION: ${{ steps.version.outputs.package_version }}
          BUILD_VERSION: ${{ steps.version.outputs.version }}
          UPSTREAM_COMMIT: ${{ steps.version.outputs.upstream_commit }}
          AMD64_HASH: ${{ steps.build.outputs.amd64_hash }}
          ARM64_HASH: ${{ steps.build.outputs.arm64_hash }}
          LOONG64_HASH: ${{ steps.build.outputs.loong64_hash }}
          PACKAGE_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          source .github/scripts/ci-common.sh
          ci_sync_branch "$PACKAGE_BRANCH"

          makefile='axonhub/Makefile'
          current_version="$(sed -n 's/^PKG_VERSION:=//p' "$makefile")"
          current_release="$(sed -n 's/^PKG_RELEASE:=//p' "$makefile")"
          current_build_version="$(sed -n 's/^AXONHUB_BUILD_VERSION:=//p' "$makefile")"

          read_hash() {
            awk -v arch="$1" '
              $0 ~ "AXONHUB_ARCH:=" arch {
                getline
                sub(/^[[:space:]]*PKG_HASH:=/, "")
                print
                exit
              }
            ' "$makefile"
          }

          release=1
          if [[ "$current_version" == "$VERSION" ]] && \
             [[ "$current_build_version" == "$BUILD_VERSION" ]]; then
            release="$current_release"
            if [[ "$(read_hash amd64)" != "$AMD64_HASH" ]] || \
               [[ "$(read_hash arm64)" != "$ARM64_HASH" ]] || \
               [[ "$(read_hash loong64)" != "$LOONG64_HASH" ]]; then
              [[ "$current_release" =~ ^[0-9]+$ ]]
              release=$((current_release + 1))
            fi
          fi

          sed -i \
            -e "s/^PKG_VERSION:=.*/PKG_VERSION:=$VERSION/" \
            -e "s/^PKG_RELEASE:=.*/PKG_RELEASE:=$release/" \
            -e "s/^AXONHUB_BUILD_VERSION:=.*/AXONHUB_BUILD_VERSION:=$BUILD_VERSION/" \
            -e "s/^AXONHUB_UPSTREAM_COMMIT:=.*/AXONHUB_UPSTREAM_COMMIT:=$UPSTREAM_COMMIT/" \
            -e "/AXONHUB_ARCH:=amd64/{n;s/PKG_HASH:=.*/PKG_HASH:=$AMD64_HASH/;}" \
            -e "/AXONHUB_ARCH:=arm64/{n;s/PKG_HASH:=.*/PKG_HASH:=$ARM64_HASH/;}" \
            -e "/AXONHUB_ARCH:=loong64/{n;s/PKG_HASH:=.*/PKG_HASH:=$LOONG64_HASH/;}" \
            "$makefile"

          grep -Fx "PKG_VERSION:=$VERSION" "$makefile"
          grep -Fx "PKG_RELEASE:=$release" "$makefile"
          grep -Fx "AXONHUB_BUILD_VERSION:=$BUILD_VERSION" "$makefile"
          grep -Fx "AXONHUB_UPSTREAM_COMMIT:=$UPSTREAM_COMMIT" "$makefile"
          grep -Fqx "  PKG_HASH:=$AMD64_HASH" "$makefile"
          grep -Fqx "  PKG_HASH:=$ARM64_HASH" "$makefile"
          grep -Fqx "  PKG_HASH:=$LOONG64_HASH" "$makefile"
          git diff --check

          ci_commit_and_push \
            "$PACKAGE_BRANCH" \
            "chore: 更新 Axonhub 至 ${BUILD_VERSION}-r${release}" \
            "$makefile"

      # Keep historical binaries: fixed-commit firmware/APK builds still
      # download their pinned release after the next daily update.

  build:
    name: 编译并发布 Axonhub 核心 APK
    needs: release
    if: >-
      always() && needs.release.outputs.package_changed == 'true' &&
      inputs.build_apks
    uses: ./.github/workflows/Build-APK-Packages.yml
    with:
      source_sha: ${{ needs.release.outputs.source_sha }}
      packages: axonhub
